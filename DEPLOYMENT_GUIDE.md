# دليل النشر على GitHub Pages 🚀

## الطريقة الأولى: رفع الملفات مباشرة

### الخطوات:

#### 1️⃣ إنشاء مستودع GitHub جديد

1. اذهب إلى: https://github.com/new
2. اسم المستودع: `school-support-form` (أو أي اسم تريده)
3. الوصف: `نظام إدارة استمارات دعم التميز المدرسي`
4. اختر: **Public** (عام)
5. ✅ لا تضف README (لدينا واحد جاهز)
6. اضغط **Create repository**

---

#### 2️⃣ رفع الملفات

##### الطريقة A: باستخدام واجهة GitHub (سهلة)

1. في صفحة المستودع، اضغط **Add file** > **Upload files**
2. اسحب جميع الملفات التالية:
   - `index.html`
   - `style.css`
   - `script.js`
   - `logo2.png`
   - `README.md`
   - `.gitignore`
   - `_config.yml`
3. اكتب رسالة commit: `إضافة ملفات النظام الأولية`
4. اضغط **Commit changes**

##### الطريقة B: باستخدام Git CLI (متقدمة)

```bash
# في مجلد الملفات
git init
git add .
git commit -m "إضافة ملفات النظام الأولية"
git branch -M main
git remote add origin https://github.com/اسم-المستخدم/school-support-form.git
git push -u origin main
```

---

#### 3️⃣ تفعيل GitHub Pages

1. في المستودع، اذهب إلى **Settings**
2. من القائمة الجانبية، اختر **Pages**
3. في قسم **Source**:
   - Branch: `main`
   - Folder: `/ (root)`
4. اضغط **Save**
5. انتظر 1-2 دقيقة ⏳
6. سيظهر رابط الموقع في أعلى الصفحة:
   ```
   Your site is live at https://اسم-المستخدم.github.io/school-support-form/
   ```

---

#### 4️⃣ اختبار الموقع

1. انسخ الرابط من GitHub Pages
2. افتحه في المتصفح
3. تأكد من عمل جميع المميزات:
   - ✅ ظهور الصفحة بشكل صحيح
   - ✅ اختيار الأسبوع والتاريخ
   - ✅ حفظ البيانات
   - ✅ المعاينة
   - ✅ التصدير PDF/Excel

---

## الطريقة الثانية: Fork المشروع (إذا كان موجوداً)

### الخطوات:

1. اذهب إلى المستودع الأصلي
2. اضغط زر **Fork** في أعلى اليمين
3. اختر حسابك
4. انتظر نسخ المستودع
5. اذهب إلى **Settings** > **Pages**
6. فعّل GitHub Pages كما في الطريقة الأولى
7. الموقع جاهز!

---

## 🎨 تخصيص الموقع

### تغيير الشعار:
1. استبدل `logo2.png` بشعارك
2. Commit التغييرات
3. انتظر 1-2 دقيقة للتحديث

### تغيير الألوان:
1. افتح `style.css`
2. ابحث عن قسم CSS Variables:
   ```css
   :root {
       --primary-color: #006341;
       --secondary-color: #d4af37;
       /* ... */
   }
   ```
3. غيّر الألوان كما تريد
4. Commit التغييرات

### تعديل النصوص:
1. افتح `index.html`
2. عدّل النصوص المطلوبة
3. Commit التغييرات

---

## 🔧 حل المشاكل الشائعة

### المشكلة: الصفحة لا تظهر (404)
**الحل:**
1. تأكد من اسم الملف: `index.html` (بحروف صغيرة)
2. تأكد من Branch: `main`
3. انتظر 5 دقائق إضافية

### المشكلة: الشعار لا يظهر
**الحل:**
1. تأكد من رفع `logo2.png`
2. تأكد من المسار في HTML: `<img src="logo2.png">`
3. تحديث الصفحة (Ctrl+F5)

### المشكلة: الخطوط لا تظهر بشكل صحيح
**الحل:**
1. تأكد من اتصال الإنترنت (الخطوط من CDN)
2. جرّب متصفح آخر
3. امسح cache المتصفح

### المشكلة: التصدير لا يعمل
**الحل:**
1. تأكد من وجود روابط CDN في `<head>`:
   ```html
   <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
   <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
   <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
   ```
2. تحديث الصفحة
3. جرّب من متصفح آخر

---

## 🌐 نطاق مخصص (Custom Domain)

إذا أردت استخدام نطاقك الخاص:

### الخطوات:

1. **في GitHub:**
   - Settings > Pages
   - Custom domain: أدخل نطاقك (مثل: `school-support.com`)
   - Save

2. **في مزود النطاق:**
   - أضف CNAME record:
     ```
     Type: CNAME
     Name: www
     Value: اسم-المستخدم.github.io
     ```
   - انتظر 24-48 ساعة للتفعيل

3. **تفعيل HTTPS:**
   - GitHub Pages يوفر HTTPS مجاناً
   - سيتم تفعيله تلقائياً بعد ربط النطاق

---

## 📊 إحصائيات الموقع

### إضافة Google Analytics (اختياري):

1. أنشئ حساب في Google Analytics
2. احصل على Tracking ID
3. أضف الكود في `<head>` من `index.html`:
   ```html
   <!-- Google Analytics -->
   <script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
   <script>
     window.dataLayer = window.dataLayer || [];
     function gtag(){dataLayer.push(arguments);}
     gtag('js', new Date());
     gtag('config', 'GA_MEASUREMENT_ID');
   </script>
   ```

---

## 🔄 التحديثات المستقبلية

### لتحديث الموقع:

1. عدّل الملفات محلياً
2. ارفع الملفات المحدثة إلى GitHub
3. أو استخدم Git:
   ```bash
   git add .
   git commit -m "تحديث: وصف التحديث"
   git push
   ```
4. انتظر 1-2 دقيقة للنشر

---

## ✅ قائمة التحقق

قبل النشر، تأكد من:

- [ ] جميع الملفات موجودة
- [ ] الشعار يظهر بشكل صحيح
- [ ] روابط CDN موجودة في HTML
- [ ] اختبار الموقع محلياً
- [ ] README محدث بمعلومات صحيحة
- [ ] لا توجد أخطاء في Console

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. راجع قسم حل المشاكل أعلاه
2. تحقق من GitHub Issues
3. اطلب المساعدة من الفريق

---

<div align="center">

**بالتوفيق! 🎉**

**صُمم بـ ❤️ للتعليم في المملكة العربية السعودية**

</div>
